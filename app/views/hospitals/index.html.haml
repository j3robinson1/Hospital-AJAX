%h3 List of hospitals
= link_to "New hospital", new_hospital_path, remote: true
%ul
  - @hospitals.each do |hospital|
    %li{class: "hospital-#{hospital.id}"}
      = hospital.name
      |
      = hospital.location
      |
      = link_to "Hospitals Patients", hospital_patients_path(hospital)
      |
      = link_to "Delete", 'javascript:void(0);', class: "delete-hospital", hospital_id: hospital.id